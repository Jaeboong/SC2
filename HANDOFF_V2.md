# HANDOFF — V2 (2026-07-23)

V1은 `HANDOFF.md`를 그대로 본다. **이 문서는 V2 전용이다.**

## 30초 요약

V1(`Custom AI v1.25.0`)은 유닛 제어·빌드 강제·생산 규칙·구매·연구를 전부
우리가 했다. 그 층이 밀레 AI를 망가뜨렸다는 것이 사용자 판단이고, V2는 그것을
통째로 걷어낸 뒤 **밀레 AI가 증명 가능하게 하지 않는 일만** 다시 채운다.

지금 V2는 **돌아가는 런처 2개**(실전·테스트)와 **보조 4종 + 캠페인 유닛**을
갖고 있다. 확장·병력·야생저그 고정은 엔진 실측으로 검증됐다. 생산건물 보조와
캠페인 유닛은 **아직 엔진 검증 전**이다(둘 다 한 판에 같이 볼 수 있다).

콘텐츠는 V1과 같은 수준까지 왔다 — 캠페인 유닛 6종(업그레이드 연결 포함)과
프로토스 진영 교체가 모두 들어 있다.

## 시작하는 법

```
start_custom_ai_v2.cmd        실전
start_custom_ai_v2_test.cmd   테스트 (자원 무한·즉시 생산·전체 시야·배속·옵저버)
```

V1 런처(`start_custom_ai.cmd`)는 그대로 살아 있고 서로 영향이 없다.
설정 파일도 별도다 — V2는 `runtime/custom_ai_v2_settings.json`.

## 반드시 먼저 읽을 것

1. **[`v2/docs/status.md`](v2/docs/status.md)** — 실측 수치, 실패 목록, 미해결 문제.
   이 문서가 V2의 실질적인 사양서다
2. [`v2/README.md`](v2/README.md) — 방향과 파일 배치
3. [`CLAUDE.md`](CLAUDE.md)의 불변식 목록 — **V1의 가장 큰 자산**. 코드가 아니라
   측정 기록이다. V2도 여기 적힌 함정을 그대로 밟는다
4. [`v2/docs/harness.md`](v2/docs/harness.md) — 검증 티어. 특히 **규칙 0**
   (계측은 사용자 실행 경로에 절대 들어가지 않는다)

## 절대 어기면 안 되는 것

- **V1 코드(`tools/` `app/` `sc2team/`)를 V2 때문에 고치지 않는다.** V2는
  `melee_only=True`로 V1 빌더를 호출만 한다. (예외: §8에 적힌 야생 저그 시작
  위치 버그 수정 — 그건 V1 자체의 버그였다)
- **릴리스 맵과 `runtime/custom_ai_settings.json`을 변형하지 않는다.** 진단은
  별도 맵·별도 설정·별도 실행기로 한다 (사용자 지침 §101)
- **프로브 코드가 사용자 런처가 굽는 맵에 들어가면 안 된다.** 돌린 뒤 릴리스
  맵에 프로브 심볼이 없는지 확인한다
- **엔진 티어 전에 SC2와 에디터를 닫는다.** 고정 포트를 쓴다

## 다음에 할 일 (우선순위 순)

### 1. 생산건물 `AIBuild` 검증 ★

방금 `AISetStock` 계열에서 `AIBuild` 직접 발주로 바꿨는데 **한 번도 엔진에서
안 돌려봤다.** 순서:

1. `python v2/verification/smoke_v2_map.py` — T2 컴파일. 5초에 유닛 0이면
   컴파일이 죽은 것이다 (`c_makePriorityTown` / `c_nearChokePoint` /
   `c_techCountCompleteOnly` 중 하나가 미선언일 수 있다)
2. 홀짝 뒤집기 2판 — 건물이 실제로 느는가
3. **§83 감시**: 다이얼 슬롯의 일꾼·기지·병력이 대조군보다 떨어지면 빌드
   매니저가 방해받은 것이다. 그 즉시 롤백

### 2. 업그레이드 원인 규명 ★

아무도 업그레이드를 안 한다. V1 §63이 같은 것을 관측했지만 **원인을 규명하지
않고 우회로만 만들었다.**

먼저 **보조를 전부 끈 순수 밀레 AI**로 한 판 돌린다. 업그레이드가 나오면
우리 병력 배율이 스톡 목록을 막고 있던 것이고, 안 나오면 밀레 AI가 원래 안
하는 것이다. 계측은 연구 건물의 `orders`로 한다(옵저버는 남의 업그레이드
목록을 못 읽는다).

원인과 무관하게 해결책은 있다 — V1의 직접 연구 명령. 명령 인덱스 표는
`tools/build/tables.cjs`에 있고 **새로 만들지 말고 그대로 가져올 것**
(잘못된 인덱스는 조용한 무동작이다).

### 3. 캠페인 유닛 검증 ★

2026-07-23에 넣었고 **엔진에서 한 번도 안 돌렸다.** 골리앗·프레데터·의무병·
애버레이션·토라스크·랩터 진화. 훈련 인덱스가 틀리면 오류가 아니라 **무동작**이라
"켰는데 안 나온다"가 유일한 증상이다.

위 §1의 판에 같이 태우면 된다. 안 나올 때 의심 순서(코드를 읽어서 고르지 말고
하나씩 풀어서 가를 것 — §91.10의 교훈):

1. 잔고 하한이 높다 — 토라스크는 광물 800 / 가스 600이 있어야 발주된다
2. 대기열 상한 2라 밀레 AI가 공장을 계속 점유한다
3. 훈련 인덱스가 틀렸다 (`tools/campaign_data/roster/AbilData.xml`이 근거)

표와 설계 이유는 `v2/docs/status.md` §5.4.

### 4. 미결 사항 (사용자 결정 필요)

- **유닛 금지**(§104의 (c)) 도입 여부 — 지금은 공중 유닛이 자유롭게 나온다
- **야생 저그 해처리 수** — 14개라 가스 경제가 안 돈다(맵 편집 필요)
- **죽은 체크박스** "수송·탐지 지원 공중 유닛 허용" 제거

## 이 세션에서 배운 함정

- **시간 기준**: 게임 시계는 `루프/16`이지 `루프/22.4`가 아니다. 초기 프로브가
  1.4배 어긋나 "9분 해제"가 8분처럼 보였다
- **홀짝 뒤집기 없이 판정하지 말 것**: 짝수 슬롯은 종족 구성 때문에 병력값이
  구조적으로 높다. 이 편향을 다이얼 효과로 오독했다
- **JS 템플릿 리터럴 안 Galaxy 주석에 백틱 금지** — 문자열이 끊긴다
- **`pack`은 공간이 모자라면 나중에 packed된 위젯을 자른다** — 하단 버튼을
  먼저 `side="bottom"`으로 잡고 표를 마지막에 `expand`로 채운다
- **선례를 먼저 읽을 것**: `AISetStock`으로 건물을 짓게 하는 길은 V1 §71이 이미
  막다른 길로 판정해 놨다. 그걸 모르고 3판을 낭비했다

## 파일 지도

```
start_custom_ai_v2.cmd            실전 진입점
start_custom_ai_v2_test.cmd       테스트 진입점
HANDOFF_V2.md                     이 문서
v2/
  README.md                       방향·파일 배치·AIBuild 방침
  docs/
    status.md                     ★ 실측 수치·실패 목록·미해결 문제
    builds.md                     밀레 AI 기본 빌드 6종
    harness.md                    검증 티어 T0~T4
  app/play_custom_ai_v2.py        런처 (V1 UI + V2 보조 줄 2개)
  sc2team_v2/
    config.py                     V2Assist — 보조 설정과 그 근거
    runtime.py                    build_v2_map (melee_only 빌드 + 보조 주입)
  tools/patch_v2_assist.cjs       Galaxy 주입기 (보조 4종)
  verification/
    smoke_v2_map.py               T2 컴파일 스모크
    probe_wild_zerg.py            야생 저그 경제·고정 계측
```

## 현재 산출물

| 항목 | 값 |
| --- | --- |
| V2 실전 맵 | `runtime/maps/europe-melee-custom-ai-v2.0.0.SC2Map` |
| V2 테스트 맵 | `…-v2.0.0-test.SC2Map` |
| V1 릴리스 맵 | 3,020,527 바이트 / `D8470D9BD22E9831D614CB3DBD30C8761A3047B47BF866093EDE9268A530D832` (무변경) |
| 오프라인 티어 | 12/12 PASS |
